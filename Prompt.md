# CONTEXT: My C Language "Cultivation AI" Evolution Project

你好，我需要你快速、全面地理解我正在进行的一个C语言项目。这个项目我们已经深入讨论和开发了很长时间，现在我需要你接替之前的工作。为了让你达到同等的理解深度，我将为你提供项目的完整上下文，包括最终代码、进化历史和我的下一个目标。

请仔细阅读并消化以下所有信息。

## 1. 项目核心目标

这是一个基于文本的、回合制的“修仙”主题对战游戏。其核心是开发一个能够通过与不同对手的对战，进行自我学习和进化的AI（我们称之为V2 AI）。我们已经成功地实现了AI的学习、权重调整、遗忘机制，并解决了大量的BUG和逻辑问题。

## 2. 项目当前状态

*   语言：C语言
*   核心玩法：回合制对战，玩家通过消耗QI（灵气）来使用各种技能。
*   AI系统：
    *   V0：随机AI，作为基准。
    *   V1A：基于硬编码规则的启发式AI，作为陪练。
    *   V2：核心的、基于权重评分的自学习AI。
*   学习机制：V2 AI通过`EvaluateAction`函数为每个可选行动打分，并在每局结束后通过`AI_Learn_From_Game`函数调整自身权重。我们已经为其加入了包括“突破潜力”、“伤害效率”、“风险规避”在内的多个高级评估维度，并实现了“权重归一化”的遗忘机制。
*   环境：代码在Windows环境下使用GCC/MinGW编译，我们已经解决了所有关于颜色代码和中文字符集的显示问题。

## 3. 最终版核心代码文件

--- START OF FILE QI.c ---

[ 在这里粘贴你的 QI.c 文件的全部内容 ]

--- END OF FILE QI.c ---

--- START OF FILE QI.h ---

[ 在这里粘贴你的 QI.h 文件的全部内容 ]

--- END OF FILE QI.h ---

--- START OF FILE weight_viewer.c ---

[ 在这里粘贴你的 weight_viewer.c 文件的全部内容 ]

--- END OF FILE weight_viewer.c ---


## 4. 项目的进化史 (我们的完整对话记录)

以下是我们之前所有的对话历史。这部分至关重要，因为它包含了我们发现和修复每一个BUG的完整过程，以及做出每一个设计决策背后的思考逻辑。请务必仔细阅读，以理解代码为何是现在的样子。

--- START OF CONVERSATION HISTORY ---

---

### 项目进化史纪要 (Condensed Conversation History)

**初始阶段：代码审查与核心BUG修复**

项目最初的版本虽然结构良好，但存在几个致命的底层BUG，我们逐一进行了修复，为后续开发奠定了稳定的基础。

*   **状态泄露**: `Game_init()`函数未能完整重置所有`Player`状态（如`burst_count`, `healing`等），导致上一局游戏的状态会“遗传”到下一局，引发了“一回合产生四百万个风刃”等崩溃性问题。**解决方案**：彻底重写`Game_init()`，确保对结构体的所有成员进行初始化。
*   **重复初始化**: `Initialize_Action_lists()`（技能列表生成）和`srand()`（随机数播种）最初被错误地放在主训练循环内。这导致了“技能已被删除”的逻辑错误，以及在高速运行时因`time(NULL)`返回值不变而丧失随机性的问题（`freopen`加速后胜率跌至0%的根源）。**解决方案**：将这两个函数都移至`main`函数的最开始，保证它们在整个程序生命周期中只执行一次。
*   **逻辑错误**:
    *   修复了`Initialize_Action_lists()`中因`switch-fallthrough`导致的“境界越高、技能越少”的逻辑颠倒问题。
    *   修复了`Burst`技能在`Oneway_Solution`中可能被重复计算伤害的BUG。

**第二阶段：AI行为进化与学习机制完善**

这是项目的核心。我们通过观察AI行为，不断迭代其“大脑”和“学习方法”。

*   **权重爆炸**: AI的`w_damage_per_point`权重一度增长到千万级别。**原因分析**：在`AI_Learn_From_Game`中使用了**乘法**进行权重更新，导致了指数级增长。**解决方案**：将所有权重更新方式从**乘法 (`*=`) 改为加法 (`+=/-=`)**，并引入了“权重范围限制(Clamping)”作为保险丝。
*   **无限僵局**: `V2`(学习型AI) 与 `V1`(启发式AI) 的对战中，出现了“治疗-攻击-打断”的无限循环。**原因分析**：`V1`的“低血量必须治疗”的僵硬逻辑，恰好触发了`V2`的“打断治疗有高额奖励”的贪心决策。**解决方案**：优化`V1`逻辑，引入“无效治疗”判断和随机性来打破僵局；同时优化`V2`的`EvaluateAction`函数，让其能够权衡“打断”的真实价值。
*   **引入高级策略维度**: 为了让AI摆脱“短期思维”，我们为其加入了全新的战略权重，使`V2` AI进化到了一个新高度：
    *   `w_qi_advantage` (资源优势感)
    *   `w_damage_per_qi` (效率感)
    *   `w_low_hp_penalty` (风险规避感)
    *   `w_breakthrough_urgency` (攒气突破的“大道之心”)
*   **引入遗忘机制**: 为了防止AI因权重过高而“思想僵化”，我们在`AI_Learn_From_Game`的末尾加入了**权重归一化（Normalization）**步骤。它能在保持权重核心比例的同时，将所有权重等比例缩小到一个合理范围，维持了AI的长期学习能力和灵活性。

**第三阶段：环境兼容性与用户体验优化**

我们解决了所有在Windows环境下运行C程序时遇到的经典“天坑”。

*   **颜色代码乱码**: **原因分析**：Windows默认CMD窗口不解析ANSI转义序列。**解决方案**：在`main`函数中调用Windows API `SetConsoleMode`并加入`ENABLE_VIRTUAL_TERMINAL_PROCESSING`标志来激活支持。为兼容旧SDK，我们最终采取了手动定义该宏值为`0x0004`的方案。
*   **中文字符乱码**: **原因分析**：源代码的**UTF-8**编码与编译器默认输出的**GBK**编码、以及VS Code终端的**UTF-8**编码之间产生了复杂的冲突。**最终解决方案**：
    1.  移除代码中运行时的`SetConsoleOutputCP`转换。
    2.  在编译时加入GCC的编译器标志 **`-fexec-charset=GBK`**，将编码转换的责任完全交给编译器。
    3.  这使得生成的`.exe`文件完美适配Windows默认环境，而在VS Code终端中测试时，只需手动执行`chcp 936`命令即可。
*   **功能优化**: 为程序加入了`clock()`计时功能和`_getch()`“任意键退出”功能。

**当前阶段：未来规划**

我们已经讨论并规划了AI能力的下一步提升方向，包括引入“对手建模”、“未来预测”、“多元化训练环境”乃至最终的“遗传算法”。

---

--- END OF CONVERSATION HISTORY ---


## 5. 我的下一个目标

在完全理解了以上所有上下文之后，我希望你帮我实现我规划的下一个功能：

[ 在这里清晰地描述你的下一个任务。例如：“现在，我准备好挑战AI能力的下一个提升方向了。请你指导我，如何在现有的代码基础上，实现‘方向一：知己知彼 —— 引入“对手建模”’这个功能。” ]

请确认你已理解所有内容，并开始为我的下一个目标提供指导。